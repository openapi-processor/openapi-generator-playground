pluginManagement {
    repositories {
        maven {
            url "https://oss.sonatype.org/content/repositories/snapshots"
        }
        gradlePluginPortal()
    }
}

rootProject.name = 'openapi-generator-playground'

//includeBuild("../openapi-processor-gradle")
